<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="fpcy.common.dao">
	<insert id="YzmDao_saveYzmLog" parameterType="map">
		INSERT
		INTO fpcy_yzmrequest_log(alert,key4,key1,pic_id,yzm,serverIP)
		VALUES(#{alert},#{key4},#{key1},#{pic_id},#{yzm},#{serverIP})
	</insert>

	<select id="YzmDao_getYzm" parameterType="map"  resultType="map">
		select fpdm from test
		<where>
		  <if test="fplx !='' and fplx !=null">fplx=#{fplx}</if>
		  <if test="qy !='' and qy !=null">and  qy=#{qy}</if>
		</where>
	</select>
	<!-- 保存请求日志 -->
	<insert id="YzmDao_saveRequestLog" parameterType="map">
		INSERT
		INTO fpcy_request_log(invoiceNum,requestId,invoiceCode,invoiceName,requestType,requestConent,
		errorMsg,errorCode,requestTime,isSuccess,comeFromCode,
		inputTime)
		VALUES(#{invoiceNum},#{requestId},#{invoiceCode},#{invoiceName},#{requestType},#{requestConent},
		#{errorMsg},#{errorCode},#{requestTime},#{isSuccess},#{comeFromCode},
		now())
	</insert>
	<!-- 保存打码日志-->
	<insert id="YzmDao_saveOcrRequestLog" parameterType="map">
		INSERT
		INTO ocr_request_log(codeType,requestId,picId,yzm,reviceTime,recogTime,
		dateLength,company,
		inputTime)
		VALUES(#{codeType},#{requestId},#{picId},#{yzm},#{reviceTime},#{recogTime},
		#{dateLength},#{company},
		now())
	</insert>
	
	<update id="YzmDao_updateOcrRequestLog" parameterType="map">
	  update ocr_request_log
	  set isRight=#{isRight},updateTime=now() 
	  where picId=#{picId}
	</update>
</mapper>